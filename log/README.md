# log模块（同步/异步日志）
## 功能
- **提供统一日志接口**:DEBUG/INFO/WARN/ERROR
- **支持两种写入模式**
  - 同步:调用线程直接写文件
  - 异步:调用线程只入队，后台写线程负责落盘
- **日志文件切分**
  - 按天(日期变化)
  - 按行数(达到split_lines)
- **单例模式**（懒汉）在第一次调用时构造唯一Log对象
- 异步日志使用了**阻塞队列**来实现